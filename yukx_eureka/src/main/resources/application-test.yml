server:
  port: 8762
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: localhost
  serviceUrl:
    defaultZone: http://yukx:123456@localhost:8762/eureka= http://${eureka.instance.hostname}:${server.port}/eureka/